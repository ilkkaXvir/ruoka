    //{this.props.image.map(function(name, index){return <div key={ index } className="col-lg-4">  <img onClick={()=>window.open("this.state.url[index]", "_blank")} src={name} className="img-responsive" alt="image" height="300" width="300"/>
            /*{this.props.image.map(function(name, index){return  })}
                  {index != 0 && index % 3 == 0 ? (</div>) : ''}
                  {index == 0 ? (<div className="row">) : ''}
                  {index != 0 && index % 3 == 0 ? (<div className="row">) : ''}
                  <div key={ index } className="col-lg-4">
                    <h3>{index}</h3>
                    {this.props.image.map(function(name, index){return <div key={ index } className="col-lg-4">  <img onClick={()=>window.open("this.state.url[index]", "_blank")} src={name} className="img-responsive" alt="image" height="300" width="300"/>
                  </div>
                })}*/